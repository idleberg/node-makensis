{
  "name": "makensis",
  "version": "0.5.0",
  "description": "A Node wrapper for makensis, the NSIS compiler",
  "main": "dist/makensis.js",
  "files": [
    "dist/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/idleberg/node-makensis.git"
  },
  "keywords": [
    "nsis",
    "makensis"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^8.0.20",
    "ava": "^0.22.0",
    "gulp": "github:gulpjs/gulp#4.0",
    "gulp-debug": "^3.1.0",
    "gulp-jsonlint": "^1.2.0",
    "gulp-tslint": "^8.1.2",
    "tslint": "^5.6.0",
    "typescript": "^2.4.2"
  },
  "scripts": {
    "build": "tsc -p ./",
    "prepublish": "gulp lint && npm run build",
    "test": "node_modules/.bin/ava ./test --verbose --serial --fail-fast"
  }
}
