{
  "name": "makensis",
  "version": "0.17.1",
  "description": "A Node wrapper for makensis, the NSIS compiler",
  "main": "dist/makensis.js",
  "files": [
    "dist/"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/idleberg/node-makensis.git"
  },
  "keywords": [
    "nsis",
    "makensis"
  ],
  "license": "MIT",
  "dependencies": {
    "@nsis/language-data": "^0.7.1"
  },
  "devDependencies": {
    "@types/node": "^12.6.2",
    "ava": "^2.2.0",
    "gulp": "^4.0.2",
    "gulp-debug": "^4.0.0",
    "gulp-jsonlint": "^1.3.0",
    "gulp-tslint": "^8.1.4",
    "husky": "^3.0.0",
    "tslint": "^5.18.0",
    "typedoc": "^0.14.2",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "build": "tsc --pretty --project ./",
    "dev": "tsc --watch --pretty --project ./",
    "lint": "tslint --project .",
    "prepublishOnly": "gulp lint && npm run build",
    "test": "ava ./test/commands.js ./test/encoding.js --verbose",
    "test:wine": "ava ./test/wine.js --verbose"
  }
}
